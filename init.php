<?php defined('SYSPATH') or die('No direct script access.');

/**
 * Detect if CMF is installed (only on development environment) and if not,
 * display install wizard.
 */
if (Kohana::$environment == Kohana::DEVELOPMENT)
{
	// TODO
}